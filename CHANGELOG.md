# 变更日志

本项目版本号遵循《钢研国创DMMA项目软件版本管理规范》：三段式 `vX.Y.Z`，生产环境版本号与 Git 标签一致，每个版本对应一次 git 版本迭代。

## v1.5.9 - 2026-09-02

### 问题修复

- **报告弹窗来源标签被拉伸成竖条**：修复多来源对比（查看报告弹窗）中，四个来源标签（知乎直答/知乎搜索/博查搜索/Tavily 搜索）点开后渲染为占满弹窗的巨型竖条、报告区域空白的问题
  - 根因（前端布局）：`.report-modal` 为 grid 布局（`grid-template-rows:auto 1fr`），多来源 batch 时弹窗内有 3 个子元素（header、`.modal-source-tabs`、`.report-body`），`1fr` 落在了来源标签行上——选中空报告来源（如知乎搜索）时报告区仅剩 `.report-empty` 的 280px，其余高度全部被标签行占据，标签按钮被 stretch 成整屏竖条；选中含报告来源时报告行按内容撑开、内部滚动同样失效
  - 修复（CSS）：`.report-modal` 由 grid 改为 flex 纵向布局——header 与来源标签行恒为内容高度（`flex:0 0 auto`），报告主体 `flex:1;min-height:0` 占满剩余高度内部滚动；四个来源点开均为「标签胶囊行 + 目录/报告内部滚动」的正常形态，空报告来源显示虚线占位提示（v1.5.8 行为保留）
- **版本号**：更新至 v1.5.9（前端资源签名 `.v141`），重新构建镜像并部署

### 版本追溯

- 修复「报告弹窗来源标签被拉伸成竖条」= v1.5.9

---

## v1.5.8 - 2026-09-02

### 问题修复

- **知乎直答报告显示异常**：修复多来源对比（查看报告弹窗按来源切换）时，「知乎直答」显示空白或引用/来源塌缩的问题（其余来源正常）
  - 根因一（后端）：直答回答包装为统一搜索结果时，所有查询共用恒定 `title="知乎直答"`、`url="https://www.zhihu.com/"`；证据与来源管道按 URL 去重后全部塌缩成一条，导致已完成的直答报告正文引用全部指向同一编号（如 7 处均为 `[[2]]`，点击均跳知乎首页）、「信息来源」章节仅第一个竞品有可点击链接，其余竞品只剩「LLM知识库」纯文本
  - 修复（`tools/web_search.py`）：每条直答结果按查询生成独立来源——标题 `知乎直答：{查询词}`、链接指向对应知乎站内搜索页，来源不再互相覆盖，引用编号各归其位
  - 根因二（前端）：批次中未完成的来源（如直答会话被取消/失败，报告为空）在报告弹窗切到该 tab 时直接渲染空白，无任何提示
  - 修复（报告弹窗）：空报告渲染占位提示——已取消显示「该来源的分析已取消，未生成报告」、失败显示「该来源的分析执行失败，未生成报告」，并说明可重新发起分析；配套 `.report-empty*` 样式（虚线占位卡片）
- **版本号**：更新至 v1.5.8（前端资源签名 `.v140`），重新构建镜像并部署

### 版本追溯

- 修复「知乎直答报告显示异常」= v1.5.8

---

## v1.5.7 - 2026-09-01

### 问题修复

- **报告生成后页面被拉得特别长**：修复分析完成后，完整报告在面板内无限撑高、整个分屏区需要滚动很久才能看全的问题
  - 根因：v1.5.4 为消除卡片内空白将面板 `max-height` 改为不限（`none`），报告越长面板越高，页面被竖向拉长
  - 修复：① 面板恢复限高（`max(420px, calc(100vh - 320px))`），长报告在面板内部滚动；② 多面板行内改为顶部对齐（`align-items:start`），各面板按自身内容取高，避免恢复限高后同排短面板被拉伸出大片空白（v1.5.4 当时的诉求不受影响）；③ 最大化面板不受限高约束，仍占满分屏区
- **版本号**：更新至 v1.5.7（前端资源签名 `.v139`），重新构建镜像并部署

### 版本追溯

- 修复「报告生成后页面下滑特别长」= v1.5.7

---

## v1.5.6 - 2026-09-01

### 问题修复

- **取消分析后历史记录一直转圈**：修复点击「取消」后，历史侧栏对应记录仍显示「分析中」转圈的问题
  - 根因一（后端）：分析处于「等待用户确认」状态时任务已结束，`cancel()` 的 `task.cancel()` 不会触发任何异常分支，DB 状态永远停留在 `running`；续跑（resume/continue）阶段被取消时也只是吞掉 CancelledError 不落库
  - 修复：新增 `_mark_cancelled()` 统一落库+通知；`cancel()` 对已结束任务直接同步落库「已取消」；`resume()`/`continue_run()` 的 CancelledError 分支同样落库
  - 根因二（前端）：单来源（非 batch）历史记录在 `PANEL_ERROR`/`PANEL_CANCEL` 时未同步状态，本地一直显示 running 直到下次轮询
  - 修复：reducer 补齐非 batch 记录状态同步；取消/关闭后立即拉取一次历史列表对账；`batchStatus` 与服务端 `_group_batches` 优先级对齐（interrupted/cancelled/error）
- **连带问题排查修复**（本轮自查发现）：
  - 取消与完成竞态：用户在分析刚完成瞬间点击取消，不会把已完成的报告状态覆盖成「已取消」（`_mark_cancelled`/`cancel` 增加 `_finished` 守卫）
  - 已取消会话的残留确认卡片提交确认会被拒绝（返回 409），不再把已取消的分析复活续跑
  - 「重新发起」中断记录改用该记录原搜索来源（此前硬编码 Tavily，来源不符且未配置 Tavily 时必然失败）
  - 启动/确认失败时展示服务端明确错误信息（如「以下搜索来源未配置 API Key：…」），不再显示原始 JSON
  - 点击历史中「等待确认」的记录不再重置面板（此前仅跳过 running，waiting 会被重连冲掉确认卡片与进度）
  - 确认卡片时序修复：此前点击确认立即把卡片标记为「已选择」，若确认请求失败（409/断网）卡片已消失且面板卡在等待确认，用户无法重试；现改为确认成功后才标记「已选择」，失败以 toast 提示并保留卡片可重试；确认按钮增加防重入（「确认中…」），双击不再触发重复提交/误标出错
  - 面板流程条支持终态：已取消/出错的面板流程条显示灰色 + 「已取消/已出错」，不再永远停留在橙色「第 X 步进行中」样式误导用户
  - 单来源分析完成后本地历史记录补齐 `status:"completed"`（此前缺失该字段）
  - 报告弹窗中无报告内容的来源（如批次中已取消的子来源）复制/下载时给出提示，不再静默产出空文件/空剪贴板
  - 后端 confirm 接口改为确认成功后才记录用户回复（此前被拒的确认也会写入 conversation，污染历史对话回放）
  - 重连耗尽不再一律误报「出错」：SSE 重连 3 次失败后先查历史记录，已完成的面板直接展示 DB 中的报告（并补建报告消息）、已取消的显示「已取消」，仅无记录/仍在运行时才提示连接中断——修复服务重启或会话被驱逐后，重连混合批次时已完成子分析被误标「出错」的问题
  - `batchStatus` 兜底与服务端 `_group_batches` 完全对齐：混合终态（如部分完成+部分取消）的批次显示「已完成」，不再回退成 running 一直转圈
- **版本号**：更新至 v1.5.6（前端资源签名 `.v138`）

### 功能优化

- **搜索模块窗口最大化**：每个分屏面板右上角新增最大化/还原按钮，最大化后面板独占整个内容区（其余窗口暂时隐藏），再次点击还原
- **搜索模块窗口随时关闭**：每个面板右上角新增关闭按钮，任意状态（分析中/等待确认/已完成/出错/已取消）均可关闭；关闭运行中的面板会同步终止后端分析并移出分屏；全部关闭后可直接发起新分析
- **搜索来源未配置不可选**：输入栏「来源」chips 按 `/api/health` 的 API Key 配置状态展示——未配置的来源置灰禁用（悬停提示「未配置 API Key，不可选择」），点击给出 toast 提示；已选来源在配置状态变化后自动校正，全部未配置时禁止发起；后端 `/start` 同步校验，未配置来源直接返回 400 并明确提示

### 版本追溯

- 修复「取消分析后历史记录一直转圈」+ 搜索模块窗口最大化/关闭 + 未配置来源置灰 = v1.5.6

---

## v1.5.5 - 2026-08-21

### 问题修复

- **点击按钮误滚动到底部**：修复底部输入表单区/历史列表等任意按钮触发重渲染时，聊天区滚动条被自动平滑拉到底部、打断用户当前浏览位置的问题
  - 根因：多面板模式下 `q0` 消息区的 `useEffect` 以 `messages`（由 `.filter()` 每次渲染生成的新数组引用）为依赖，任何按钮触发的重渲染都会使依赖引用变化，从而执行 `scrollIntoView` 把滚动容器拉到最底部
  - 修复：① 仅在消息**内容真实变化**（条数 / 末条 id / 末条内容长度）时才自动滚动；② 增加就近底部判断——若用户已向上滚动阅读，则不再强行拉到底部；③ 自动滚动仍保留在原滚动容器（面板区/聊天区）内，不误滚页面
- **版本号**：更新至 v1.5.5（前端资源签名 `.v137`），重新构建镜像并部署

### 版本追溯

- 修复「点击按钮后滚动条自动跳到底部」问题 = v1.5.5

---

## v1.5.4 - 2026-08-21

### 底部输入表单区美化

针对输入表单区的 8 项美观问题（字号层级倒置、控件高度不齐、textarea resize 角标、来源 chips 悬空、默认模版提示条过重、发送按钮全宽压迫、纵向间距过大、右侧空白浪费）做整体打磨，保持米色纸张主题与圆角风格：

- **字号层级恢复**：标签统一 13px 加粗、输入/下拉/文本域内容 14px、发送按钮 15px、提示文字 12px，恢复「标签 ≥ 内容」的正常层级
- **控件尺寸规范化**：单行控件（赛道输入、模版下拉）高度统一 42px；竞品 textarea 固定高度 100px 且 `resize:none`，去除右下角拖拽角标
- **布局重排（方案 A）**：第一行 = 赛道 / 模版 / 来源（chips 垂直居中、紧凑对齐），第二行 = 竞品文本域独占整行，消除 chips 下方大块悬空与右侧空白
- **默认模版维度提示条轻量化**：去掉厚重灰盒，改为 12px 灰色小字 + ⓘ 图标，视觉重量明显轻于输入控件
- **发送按钮收敛**：由粗条改为右对齐、200×46 圆角按钮（宽 160–240px、高 44–48px 区间），不再全宽压迫；版本徽章保持固定在页面右下角，不与按钮同行争位
- **间距节奏紧凑**：行内列距 18px、块间纵向间距 12–14px，整体更紧凑
- **版本号**：更新至 v1.5.4（前端资源签名 `.v136`），重新构建镜像并部署

### 版本追溯

- 底部输入表单区美观优化 = v1.5.4

---

## v1.5.3 - 2026-08-21

### 本对话更新汇总

本次迭代（v1.5.0 → v1.5.3）完整变更，除下述汇总外，各版本细节见下方 v1.5.0 / v1.5.1 / v1.5.2 条目。

- **功能（v1.5.0）**
  - 广度/深度合并：移除「深度」选择与深度确认步骤，统一完整模式（含反思补采）执行
  - 新增分析模版：默认模版 + 自定义模版（维度/关键词，可增删），`/api/templates` CRUD + `/start` 支持 `template_id`/`template`；研究计划按所选模版维度/关键词精确使用
  - 搜索引擎来源移除「自动」选项，缺省 Tavily（服务端 `SEARCH_PROVIDER` 内部回退保留）
- **修复一（v1.5.1）**
  - 残留「分析中」清理：启动/停机把 running 批量标记为 `interrupted`，前端显示「已中断」不再转圈
  - 历史对账：前端 15s 轮询 + 重新聚焦刷新
  - 任意状态可删除（运行中先终止），删除/复制均有 toast 反馈
  - 发送防重复（点击即禁用）、状态文案完整展示、来源提示「可多选」、时间戳 UTC 带偏移
- **修复二（v1.5.2）**
  - 一键「清理已中断」批量删除（`POST /api/analysis/history/cleanup`）+ 列表即时刷新
  - 已中断记录「重新发起」（重试）入口
  - 发送防重复加强（await 到面板创建完成前保持禁用）
  - 竞品文本域 `resize:vertical` 防横向变形、模版维度提示条去伪光标且只读、历史细滚动条
- **发布（v1.5.3）**
  - 版本号更新至 v1.5.3（前端资源签名 `.v135`），重新构建镜像并部署

### 版本追溯

- v1.5.3 发布（与 Git 标签一致），汇总本对话全部更新

## v1.5.2 - 2026-08-21

### 功能新增 / 优化

- **一键清理已中断记录**：历史侧栏新增「清理已中断」按钮（`POST /api/analysis/history/cleanup`），可批量删除全部 `interrupted` 单记录及其所属整批；点击二次确认 → 删除中 loading → toast「已清理 N 条」→ 列表即时刷新
- **已中断记录可续作**：历史「已中断」记录新增「重新发起」（重试）按钮，一键按原赛道/竞品参数（默认模版 + Tavily）重新发起分析
- **发送防重复加强**：发送按钮点击后等待启动完成（await 到面板创建）期间保持禁用并显示「发送中…」，彻底杜绝连点建多条

### 问题修复

- **历史重复残留清理机制**：提供「清理已中断」批量删除入口，重复脏数据可一次性清空
- **竞品文本域**：强制 `.chat-input label textarea{resize:vertical !important}`，不可横向拖拽变形
- **默认模版维度提示条**：只读提示去除疑似光标（「…」改为「等」）+ `user-select:none`，不再可编辑/聚焦
- **历史滚动条**：`.history-panel` 加细滚动条（6px）并兼容 Firefox（`scrollbar-width:thin`）

### 版本追溯

- 一键清理已中断 + 中断可重新发起 + 防重复加强 = v1.5.2

## v1.5.1 - 2026-08-21

### 问题修复

- **残留「分析中」状态清理**：后端启动时把所有仍为 running 的旧任务批量标记为 `interrupted`（已中断），优雅停机时同样回写；前端对 `interrupted` 状态停止转圈并显示「已中断」
- **历史状态对账**：前端每 15 秒轮询一次历史列表 + 页面重新聚焦时刷新，任务状态与真实运行情况保持一致
- **删除任意状态记录**：删除接口支持任意状态（运行中先终止会话再删除），并返回明确错误信息；前端删除按钮始终可点，点击后 loading → toast 成功/失败提示，成功后即时刷新列表
- **复制反馈**：复制报告增加 toast 提示（无报告时提示「暂无报告内容」）
- **发送防重复**：发送按钮点击后立即 loading 并禁用，连点只创建一条任务；分析运行期间发送按钮保持禁用
- **来源提示文案**：「多选，每窗口一来源」改为「可多选」
- **状态文案完整展示**：「分析中…」改为「分析中」且不被截断；历史列表滚动条改细样式
- **时间戳核对**：服务端时区为 Asia/Shanghai（CST +8）、系统时间 2026-08-21，历史显示 2026 年与服务器时钟一致

### 版本追溯

- 残留 running 清理 + 任意状态删除 + 前端轮询/toast/防重复 = v1.5.1

## v1.5.0 - 2026-08-21

### 功能新增

- **广度与深度合并**：移除输入栏「深度」选项与运行中的「选择分析深度」确认步骤，分析统一按完整模式（含反思补采）执行；`depth` 保留为内部字段（历史/旧调用兼容）
- **分析模版**：输入栏「深度」改为「模版」选择（默认模版 / 自定义模版 / 新建自定义），用户可选择默认模版或自定义模版，自定义模版可增减维度和关键词
  - 新增后端 `templates` 模块 + `/api/templates` CRUD（默认模版只读，`GET/POST/PUT/DELETE` 管理自定义模版）
  - SQLite 新增 `templates` 表（维度/关键词持久化，旧库自动建表）
  - `/api/analysis/start` 新增 `template_id`（引用保存的模版）与 `template`（内联自定义模版：维度+关键词）；所选模版的维度/关键词将作为研究计划的调研重点精确使用
- **搜索引擎来源移除「自动」**：前端「来源」不再提供「自动」选项，缺省默认 Tavily；后端 `ALLOWED_PROVIDERS` 移除 `auto`，缺省 `DEFAULT_PROVIDER=tavily`（服务端 `SEARCH_PROVIDER` 内部 auto 回退逻辑保留）

### 版本追溯

- 模版自定义维度/关键词 + 广度深度合并 + 移除自动来源 = v1.5.0

## v1.4.0 - 2026-08-20

### 功能新增

- 多选搜索引擎 + 同页分屏对比：输入栏「来源」由单选下拉改为多选（自动/Tavily/博查/知乎/知乎直答），可勾选多个来源；发送一次内容即按每个来源各启动一个分析会话**并行运行**，同一页面划分为多个分析窗口并排展示各自进度与报告，方便对比不同来源的结果
- 后端 `/api/analysis/start` 新增 `search_providers`（数组）字段：按所选来源一次创建多个会话（各自独立 asyncio 任务并行），响应返回 `sessions:[{analysis_id, search_provider}]`；单值 `search_provider` 保持兼容
- 历史记录持久化 `search_provider` 字段（含 SQLite 迁移，旧库自动补列），历史详情/恢复会话可回显搜索来源标签

### 问题修复（同版本热修）

- 卡片步骤条与计数重叠：分屏窗口内改用**紧凑版步骤条**（仅数字圆圈+当前步骤名，计数移至下方独立行），任何宽度不再重叠
- 赛道关键词 chip：改为卡片头部左对齐小号 chip（副标题），不再悬浮孤立；卡片高度改为内容自适应，消除步骤条与状态条之间空白
- 底部「分析中」按钮单行显示（nowrap + min-width）；来源 chips 加宽来源列使 5 个 chip 一行放下
- 历史记录去重：多来源一次任务在侧栏只显示**一条**记录（副标题展示来源列表，如「博查搜索 / Tavily 搜索 / 知乎搜索 / 知乎直答」），后端 `/history/list` 按 `batch_id` 分组、详情/删除支持整批
- 报告弹窗支持多来源切换：点击批量记录后顶部出现来源 Tab，可切换查看各来源报告

### 版本追溯

- 多选搜索引擎分屏对比 = v1.4.0

## v1.3.0 - 2026-08-19

### 功能新增

- 新增博查（Bocha）Web Search API 搜索引擎：`BOCHA_API_KEY` 配置 + `tools/bocha_search.py`（POST `https://api.bochaai.com/v1/web-search`，Bearer 鉴权，`summary:true` 返回综合摘要），结果归一化为与其他来源一致的结构
- 搜索来源新增 `bocha` 选项：前端「来源」下拉可选「博查搜索」，后端 `SEARCH_PROVIDER=bocha` 可强制指定；auto 优先级调整为 bocha → tavily → zhihu
- `/api/health` 新增 `bocha_configured` 检查项

### 问题修复

- 博查 429 限流加固：识别 429 按 `Retry-After`/指数退避（2s→5s）最多重试 3 次，并发从 8 降到 4 降低触发概率；auto 模式下博查限流/失败自动回退 tavily → zhihu，避免分析缺搜索证据（显式选择博查时保持来源可控、不回退）

### 版本追溯

- 博查搜索引擎接入 = v1.3.0

## v1.2.1 - 2026-08-12

### 功能新增

- 对话页顶部新增分析流程图：7 步流程（输入赛道/研究规划/竞品发现/信息采集/反思评估/对比分析/报告生成），已完成节点绿色 ✓、当前节点橙色高亮，右侧显示「第 X / 共 7 步」
- 版本标识按环境差异化：生产环境可点击，弹窗展示更新日志（历史版本按版本号倒序、可滚动）；测试/预发环境无点击交互，静态展示「XX 环境 vX.Y.Z-阶段标识.序号」并以警示橙 `#FF7D00` 标识（12px/行高 18px）
- 新增 `/api/changelog` 接口：更新日志由服务端按 CHANGELOG.md 下发，标记当前版本
- 历史记录共享模式：不做账号体系，所有访问者共享同一份历史（SQLite 持久化），换设备/浏览器可见全部记录

### 版本追溯

- 流程图 = v1.2.1，版本标识环境差异化 = v1.2.1（与 Git 标签一致）

## v1.2.0 - 2026-08-11

### 功能新增

- 前端页面右下角展示版本号（12px/#86909C/距右下 20px），点击弹窗查看更新日志
- 新增 `/api/version` 接口，版本号由部署环境 `APP_VERSION` 注入（与 Git 标签保持一致）
- `deploy.sh` 支持 `VERSION` 环境变量覆盖版本号
- 新增 CHANGELOG.md，按版本倒序记录变更

### 版本追溯

- 本仓库历史版本追溯打标：init=v1.0.0，镜像固化=v1.0.1，dist修复=v1.0.2，知乎支持=v1.1.0，回放修复=v1.1.1

## v1.1.1 - 2026-08-11

### 问题修复

- 修复确认细分赛道/竞品后界面"从头开始"显示：SSE 重连回放仅保留最新 progress/thinking 事件，历史进度不再重复渲染

## v1.1.0 - 2026-08-11

### 功能新增

- 新增知乎开放平台搜索能力：全网搜索 / 知乎站内搜索 / 知乎直答
- 新增 `SEARCH_PROVIDER` 配置（auto/tavily/zhihu/zhihu_site/zhida），前端可选择搜索来源
- 数据卷挂载路径统一为 `/data`

## v1.0.2 - 2026-08-11

### 问题修复

- 修复前端 dist 加载路径（`parents[1]`），固化无超时修改到镜像

## v1.0.1 - 2026-08-11

### 维护变更

- 镜像固化无超时修改，改用 Git LFS 管理镜像包

## v1.0.0 - 2026-08-11

首个版本。

### 功能新增

- 竞品分析全流程：赛道/竞品输入 → 研究计划 → 竞品发现与确认 → 采集 → 反思补采 → 对比分析与综合洞察 → Markdown 报告
- 对话式确认：细分赛道、竞品列表、分析深度
- 历史记录回放、引用来源、预计耗时等（详见功能迭代）

### 部署

- Docker 一键部署（deploy.sh + 环境配置模板）
